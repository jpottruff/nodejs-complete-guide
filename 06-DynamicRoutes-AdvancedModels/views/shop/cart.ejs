<%- include('../includes/head.ejs') %>
    </head>

    <body>
        <%- include('../includes/navigation.ejs') %>
        <main>
            <% if (products.length > 0) { %>
                <ul>
                    <% products.forEach(item => { %>
                        <li><%= item.productData.title %> (<%= item.qty %>)</li>
                    <% }) %>
                </ul>
            <% } else { %>
                <h1>No Products in cart</h1>
            <% } %>
        </main>
        <%- include('../includes/end.ejs') %>